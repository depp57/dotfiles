- name: Machine setup
  hosts: localhost
  become: true

  tasks:
    - name: Install packages
      package:
        name:
          - curl
          - git
          - htop
          - vim
          - zsh
        state: present
